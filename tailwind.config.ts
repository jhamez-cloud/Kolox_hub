// tailwind.config.js
module.exports = {
    theme: {
        extend: {
            fontFamily: {
                mono: ['var(--font-geist-mono)'],
                // you can also add 'sans': ['var(--font-geist-sans)'] if needed
            },
        },
    },
};
